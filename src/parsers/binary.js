/**
 * Parse binary data with stream support
 * @param {Buffer} buffer - Buffer containing binary data
 * @param {object} options - Parser options
 * @returns {Buffer} Binary buffer
 */
function parseBinary(buffer, options = {}) {
  return buffer;
}

export default parseBinary;